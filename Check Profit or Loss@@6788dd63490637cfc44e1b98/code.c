#include <stdio.h>

int main() {
    int cost price;
    int selling price;
    scanf("%d %d", &cost price, &selling price );
    if(selling price > cost price){
        prinf("Profit");
    }
    else if(cost price> selling price){
        printf("Loss");
    }
    return 0;
}